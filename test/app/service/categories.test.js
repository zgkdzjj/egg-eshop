'use strict';

const { app, assert } = require('egg-mock/bootstrap');

describe('test/app/service/categories.test.js', () => {
  let ctx;
  beforeEach(() => {
    ctx = app.mockContext();
  });

  describe('create()', () => {
    it('should create successfully', async () => {
        
    })
  });

});

